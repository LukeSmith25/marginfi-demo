# Environment Variables for Solana and Marginfi

# Solana RPC Endpoint
# Switch between testnet and devnet by uncommenting the appropriate RPC_URL line.

# For testnet (currently set to testnet)
#RPC_URL=https://api.testnet.solana.com

# For devnet (uncomment this line if you want to switch to devnet)
RPC_URL=https://api.devnet.solana.com

# Wallet Private Key (this is just an example; replace it with your actual private key)
PRIVATE_KEY=

# Marginfi Environment (use "dev" for devnet, "production" for mainnet)
MARGINFI_ENV=dev  # or use "production" if running in production

# Additional environment variables can be added as needed